# AI 작업 가이드

## 목적
- 이 문서는 이 저장소에서 AI 에이전트가 따라야 할 **작업 절차와 운영 규칙**을 정의합니다.
- Unity 구현 방식, 기존 시스템 활용법, 성능 규칙은 루트의 `SKILL.md`를 우선 참조합니다.

## 개발 프로세스

### 1. 요구사항 접수
- 사용자 요청을 확인하고 범위를 정리합니다.

### 2. 개발 문서 작성 및 제시
- 구현 전에 반드시 **개발 문서**를 먼저 작성하여 사용자에게 제시합니다.
- 개발 문서에는 다음 내용을 포함합니다:
  - 기능 개요
  - 구현 방법 및 접근 방식
  - 파일 구조 및 클래스 설계
  - 주요 메서드 및 로직 설명
  - 예상되는 영향 범위

### 3. 검토 및 승인
- 사용자가 개발 문서를 검토합니다.
- 수정 요청이 있으면 문서를 갱신합니다.
- 최종 승인을 받은 뒤 구현을 시작합니다.

### 4. 구현
- 승인된 개발 문서를 기준으로 코드를 작성합니다.
- 구현 방식은 `SKILL.md`와 프로젝트 기존 구조를 우선 따릅니다.

### 5. 예외사항 보고
- 구현 중 아래 항목이 발생하면 반드시 사용자에게 보고합니다.
  - 임시 처리
  - 우회 구현
  - 미완료 항목
  - 데이터 미적용
  - 로컬라이제이션 미적용
  - 테스트 미실행
  - 에디터 후속 작업 필요사항
- 보고 시에는 누락 없이 **리스트 형태**로 정리합니다.
- 사용자 경험에 직접 영향을 주는 항목은 완료 보고 전에 먼저 알립니다.

---

## 코딩 및 문서 규칙

### 객체지향 개발 원칙
- **SOLID 원칙**을 준수합니다.
  - Single Responsibility: 클래스는 하나의 책임만 가집니다.
  - Open/Closed: 확장에는 열려있고 수정에는 닫혀있어야 합니다.
  - Liskov Substitution: 파생 클래스는 기반 클래스를 대체할 수 있어야 합니다.
  - Interface Segregation: 클라이언트가 사용하지 않는 메서드에 의존하지 않아야 합니다.
  - Dependency Inversion: 추상화에 의존해야 하며, 구체화에 의존하지 않아야 합니다.

### 코드 품질
- 유지보수 용이성을 우선합니다.
- 코드 의도가 드러나는 명확한 네이밍을 사용합니다.
- 프로젝트 전반의 코딩 스타일 일관성을 유지합니다.

### 네이밍 규칙
- **클래스명**: PascalCase
- **메서드명**: PascalCase
- **public 변수**: PascalCase
- **private 변수**: camelCase
- **private backing field**: `_camelCase`
  - public 프로퍼티에서 반환하는 private 필드에만 언더바를 사용합니다.
- **상수명**: UPPER_SNAKE_CASE

```csharp
public class Player
{
    private int _health;
    public int Health
    {
        get => _health;
        private set => _health = value;
    }

    private float moveSpeed;
    public string PlayerName;

    private const int MAX_HEALTH = 100;
}
```

### 주석 규칙
- 주석은 **한글**로 작성합니다.
- 코드의 동작 자체보다 **의도와 이유**를 설명합니다.
- 아래 대상에는 주석 작성을 우선 고려합니다.
  - 복잡한 로직
  - 비즈니스 규칙
  - 외부 의존성이나 제약사항
  - public 메서드 및 클래스

```csharp
/// <summary>
/// 플레이어의 체력을 감소시킵니다.
/// </summary>
/// <param name="damage">받을 데미지 값</param>
/// <returns>플레이어가 생존 중이면 true, 사망하면 false</returns>
public bool TakeDamage(float damage)
{
    // 체력이 0 이하로 떨어지지 않도록 보정
    _health = Mathf.Max(0, _health - damage);
    return _health > 0;
}
```

---

## Git 커밋 규칙

### 커밋 메시지 언어
- **한글**로 작성합니다.

### 커밋 메시지 형식
```text
type: 간단한 설명

상세 설명 (선택사항)
```

### 커밋 타입
- `feat:` - 새로운 기능 추가
- `fix:` - 버그 수정
- `refactor:` - 코드 구조 개선
- `docs:` - 문서 추가/수정
- `test:` - 테스트 코드 추가/수정
- `style:` - 코드 포매팅, 세미콜론 누락 등
- `chore:` - 빌드, 설정 파일 수정

### 커밋 예시
```text
feat: 플레이어 점프 기능 추가

- Jump() 메서드 구현
- 공중에서 점프 불가능하도록 isGrounded 체크 추가
```

---

## 문서 참조 규칙
- 구현 패턴과 시스템 활용법은 루트의 `SKILL.md`를 먼저 확인합니다.
- 세부 시스템 설명은 `Assets/Scripts/README.md`와 `Assets/Docs` 문서를 참조합니다.
- 새로운 프로젝트 규칙을 추가할 때는 아래 기준으로 위치를 결정합니다.
  - 작업 절차, 보고 방식, 문서 정책: `AGENTS.md`
  - Unity 구현 방식, 성능 규칙, 시스템 활용법: `SKILL.md`

## 완료 전 확인사항
- 개발 문서에 명시된 기능이 모두 반영되었는가?
- 예외사항 보고가 필요한 항목을 누락 없이 정리했는가?
- 주석이 한글로 작성되었는가?
- 변수명과 클래스명이 영어 네이밍 규칙을 따르는가?
- 커밋 메시지 형식이 프로젝트 규칙에 맞는가?

## 인코딩 규칙
- 한글이 포함된 `.md`, `.csv`, `.cs`, `.json`, `.txt`, `.yaml`, `.yml` 파일은 **UTF-8(BOM)** 으로 저장합니다.
- 한글 데이터가 포함된 파일을 수정할 때는 인코딩이 유지되는 방식으로 편집합니다.
- 인코딩 변환으로 한글이 깨졌다면 즉시 원본을 복구한 뒤 다시 작업합니다.
